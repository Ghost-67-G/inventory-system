# .env.production.example
# Copy to .env.production and fill in real values.
#
# This project expects the shared infra (Mongo + Caddy) to already be
# running on the VPS — see infra/README.md.

# Application
NODE_ENV=production
PORT=3000
DEPLOYMENT_MODE=saas
APP_VERSION=latest
GHCR_NAMESPACE=your-github-org

# MongoDB (provided by shared infra on the `shared_db` network)
# App user must match what infra/mongo-init.js seeded.
MONGODB_URL=mongodb://inventory_app:CHANGE_ME_APP_PASSWORD@mongodb:27017/inventory?authSource=inventory
MONGO_APP_USERNAME=inventory_app
MONGO_APP_PASSWORD=CHANGE_ME_APP_PASSWORD

# JWT (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_ACCESS_SECRET=GENERATE_64_BYTE_HEX_SECRET
JWT_REFRESH_SECRET=GENERATE_DIFFERENT_64_BYTE_HEX_SECRET
JWT_ACCESS_EXPIRATION_MINUTES=30
JWT_REFRESH_EXPIRATION_DAYS=30

# Redis (per-project, not shared)
REDIS_URL=redis://:REDIS_PASSWORD@redis:6379
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD

# MeiliSearch (per-project, not shared)
MEILISEARCH_URL=http://meilisearch:7700
MEILISEARCH_KEY=CHANGE_ME_AT_LEAST_16_CHARS

# Email (Resend)
RESEND_API_KEY=re_XXXXXXXXXXXX
EMAIL_FROM=noreply@codingstack.site

# Frontend / Socket.IO
FRONTEND_URL=https://stock.codingstack.site
SOCKET_CORS_ORIGIN=https://stock.codingstack.site

# Frontend build args (baked into the Vite bundle at build time)
VITE_API_URL=https://apistock.codingstack.site/api
VITE_SOCKET_URL=https://apistock.codingstack.site
